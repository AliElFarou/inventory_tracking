<h1>Items</h1>

<%= button_to "Add new item", new_item_path, method: :get %>

<ul>
    <% @items.each do |item| %>
        <li><%= link_to item.name, item_path(item)%>, <%= item.description %></li>
    <% end %>
</ul>

<%= button_to "View Warehouses", warehouses_path, method: :get %>

